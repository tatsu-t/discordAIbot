services:
  scraperapi:
    build: ./scraper
    container_name: scraper-api
    ports:
      - "8000:8000"

  bot:
    build: ./bot
    container_name: discord-bot
    env_file:
      - ./bot/.env
    environment:
      SCRAPER_API_URL: http://scraperapi:8000
    depends_on:
      - scraperapi
